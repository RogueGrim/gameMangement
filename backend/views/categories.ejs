<%# component for showing categories page %>
<%- include('navbar') %>
<section>
    <% data.forEach(item=>{ %>
        <h1><%= item.genre %></h1>
        <p><%= item.count %></p>
    <% }) %>
</section>