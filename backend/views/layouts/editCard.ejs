<%# ejs module to render cards in Management  %> 
<% data.forEach(item =>{ %>
    <div class="card">
    <p><%= item.game_name %></p>
    <p><%= item.dev_name %></p>
    <p><%= item.genre %></p>
    <button>Edit</button>
    <button>Delete</button>
    </div>
<% }) %>
