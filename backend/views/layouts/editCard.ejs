<%# ejs module to render cards in Management  %> 
<% data.forEach(item =>{ %>
    <div class="card">
    <p><%= item.game_name %></p>
    <p><%= item.dev_name %></p>
    <p><%= item.genre %></p>

    <form action="/management/edit/<%= item.id %>" method="get">
        <button>Edit</button>
    </form>

    <form action="/management/delete/<%= item.id %>" method="get">
        <button>delete</button>
    </form>
    
    </div>
<% }) %>
